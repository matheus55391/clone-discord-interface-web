import { Container, ChatButton } from "./styles"

export const ChannelList = () =>{
    return(
        <Container>
            <ChatButton>Chat</ChatButton>

            
        </Container>
    )
}