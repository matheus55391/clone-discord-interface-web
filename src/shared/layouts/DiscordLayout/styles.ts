import styled from "styled-components";

export const Grid = styled.div`
    display: grid;

    grid-template-columns: 71px auto;

    grid-template-areas:
        'SL RESTO'
        'SL RESTO'
        'SL RESTO'
    ;

    height: 100vh;
`